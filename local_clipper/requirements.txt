# CustosAI Clipper — Pinned Dependencies
# Python 3.10+
#
# MANDATORY for build (PyInstaller bundles these into the app):
#   - numpy          → video/audio, AI energy analysis (RMS)
#   - llama-cpp-python → AI clip selection (Llama 3.2-1B)

# ── GUI ──────────────────────────────────────────────────────────────
customtkinter==5.2.1
Pillow==10.4.0

# ── Video / Audio Engine (numpy REQUIRED) ────────────────────────────
numpy>=1.24.0,<2.0.0
moviepy==1.0.3
static-ffmpeg>=3.0
ffmpeg-python==0.2.0
imageio[ffmpeg]==2.34.0
opencv-python-headless>=4.10.0

# ── AI Transcription ─────────────────────────────────────────────────
faster-whisper==1.0.3
ctranslate2==4.4.0

# ── AI Clip Selection (llama-cpp-python REQUIRED) ─────────────────────
llama-cpp-python>=0.2.0
huggingface_hub>=0.20.0

# ── YouTube Download ─────────────────────────────────────────────────
yt-dlp>=2024.1.0

# ── Networking & Security ────────────────────────────────────────────
requests==2.32.3
urllib3>=1.26.0,<2.0.0
python-dotenv==1.0.1

# ── Packaging & Distribution ─────────────────────────────────────────
setuptools>=68.0.0  # Necesario para pkg_resources (ctranslate2)

# ── Packaging (install separately in build env) ─────────────────────
# pyinstaller==6.9.0
# pyarmor==8.5.11
